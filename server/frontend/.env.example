# Frontend (consumer) environment template. Copy to `server/frontend/.env`.

VITE_APP_VERSION=local
VITE_APP_CHANNEL=web
# Point to backend API (can be relative if front and api share origin)
VITE_API_BASE=/api/v1
